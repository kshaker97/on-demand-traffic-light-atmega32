/*
 * dio_test.c
 *
 *  Created on: Oct 18, 2022
 *      Author: khaled
 */
#include "dio.h"
/*
int main(void){
	while(1){
		//DIO DRIVER TEST CASES

		//TEST CASE 1:
		DIO_init(PORT_A, BIT0, OUTPUT);
		DIO_write(PORT_A, BIT0, HIGH);

		//TEST CASE 2:
		DIO_init(PORT_A, BIT0, OUTPUT);
		DIO_write(PORT_A, BIT0, LOW);

		//TEST CASE 3:
		uint8_t button;
		DIO_init(PORT_B, BIT0, INPUT);
		DIO_read(PORT_B, BIT0, &button);
		DIO_init(PORT_A, BIT0, OUTPUT);
		if(button==HIGH){
			DIO_write(PORT_A, BIT0, HIGH);
		}
		//TEST CASE 4:
		uint8_t button;
		DIO_init(PORT_B, BIT0, INPUT);
		DIO_read(PORT_B, BIT0, &button);
		DIO_init(PORT_A, BIT0, OUTPUT);
		if(button==HIGH){
			DIO_toggle(PORT_A, BIT0, HIGH);
		}
	}
}
*/


