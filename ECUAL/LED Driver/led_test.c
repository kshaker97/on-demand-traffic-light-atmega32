/*
 * led_test.c
 *
 *  Created on: Oct 18, 2022
 *      Author: khaled
 */
#include "led.h"
/*
int main(void){
	TIMER0_init();
	while(1){
		//LED DRIVER TEST CASES
		//TEST CASE 1:
		LED_init(PORT_A, BIT0);
		LED_on(PORT_A, BIT0);

		//TEST CASE 2:
		LED_init(PORT_A, BIT0);
		LED_off(PORT_A, BIT0);

		//TEST CASE 4:
		LED_init(PORT_A, BIT0);
		LED_toggle(PORT_A, BIT0);
		TIMER0_delay(1);

		//TEST CASE 4:
		LED_init(PORT_A, BIT0);
		DIO_init(PORT_B, BIT0, INPUT);
		uint8_t button;
		LED_state(PORT_B, BIT0, &button);
		if(button==ON){
			LED_on(PORT_A, BIT0);
		}
	}
}
*/
